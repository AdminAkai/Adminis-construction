import styled from 'styled-components'

export const AdminisContainer = styled.h1`
  position: relative;
  user-select: none;
  border: 3px solid ${({ theme }) => theme.Adminis};
  padding: 6px 4px 4px 4px;
`
